---
title: "עדי דוד | עיסוי רפואי"
lang: he

format:
  html:
    theme: cosmo
    css: styles.css
    toc: false
    smooth-scroll: true
    dir: rtl
---

```{=html}
<!-- Hero Section -->
<section class="hero">
  <div class="overlay"></div>
  <div class="content fade-in">
    <h1>עדי דוד – עיסוי רפואי ממוקד</h1>
    <p class="tagline">
      סובלים מכאבי גב, צוואר או עומס שרירי? טיפול מקצועי שמחזיר את הגוף לאיזון ולתפקוד מלא.
    </p>
  </div>
</section>

<!-- About Section -->
<section class="about fade-in">
  <div class="container">
    <h2>נעים להכיר</h2>
    <p>
      שמי <strong>עדי דוד</strong>, מטפלת מוסמכת בעיסוי רפואי, המתמחה בטיפול בכאבים כרוניים,
      עומסים שריריים ופציעות הנובעות משגרת חיים אינטנסיבית.
    </p>
    <p>
      כל טיפול מתחיל באבחון והתאמה אישית, וממוקד בשורש הבעיה – לא רק בסימפטומים.
      המטרה היא להקל על הכאב, לשפר טווחי תנועה, ולהחזיר אתכם לתחושת יציבות ושליטה בגוף.
    </p>
  </div>
</section>

<!-- Footer -->
<footer dir="ltr">
  <p>© 2026 Adi David · Medical Massage Therapy</p>
</footer>
```

<script>
  const faders = document.querySelectorAll('.fade-in');
  const appearOptions = { threshold: 0.2, rootMargin: "0px 0px -100px 0px" };
  const appearOnScroll = new IntersectionObserver(function(entries, observer) {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('visible');
      observer.unobserve(entry.target);
    });
  }, appearOptions);
  faders.forEach(fader => appearOnScroll.observe(fader));
  </script>
    
    
    