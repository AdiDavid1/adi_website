---
title: "Adi David | Medical Massage Therapy"
format:
  html:
    theme: cosmo
    css: styles.css
    toc: false
    smooth-scroll: true
---

```{=html}
<!-- Hero Section -->
<section class="hero">
  <div class="overlay"></div>
  <div class="content fade-in">
    <h1>Adi David</h1>
    <p class="tagline">Professional medical massage for healing, balance, and recovery</p>
  </div>
</section>

<!-- About Section -->
<section class="about fade-in">
  <div class="container">
    <h2>Welcome</h2>
    <p>
      My name is <strong>Adi David</strong>, a certified medical massage therapist dedicated to helping people 
      recover from pain, stress, and physical imbalances through evidence-based bodywork and personalized care.
    </p>
    <p>
      Each session is tailored to your unique needs — whether you’re dealing with chronic pain, muscle tension, 
      or simply looking to restore calm and well-being.
    </p>
  </div>
</section>

<!-- Footer -->
<footer>
  <p>© 2025 Adi David · Medical Massage Therapy</p>
</footer>
```

<script>
  const faders = document.querySelectorAll('.fade-in');
  const appearOptions = { threshold: 0.2, rootMargin: "0px 0px -100px 0px" };
  const appearOnScroll = new IntersectionObserver(function(entries, observer) {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('visible');
      observer.unobserve(entry.target);
    });
  }, appearOptions);
  faders.forEach(fader => appearOnScroll.observe(fader));
  </script>
  
<script>
// Mobile-friendly parallax effect
document.addEventListener('scroll', function() {
  const hero = document.querySelector('.hero');
  if (!hero) return;
  let scrollY = window.scrollY;
  hero.style.backgroundPositionY = `${scrollY * 0.5}px`;
});
</script>

    